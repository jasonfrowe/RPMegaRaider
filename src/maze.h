#ifndef MAZE_H
#define MAZE_H

// Generates a random open-cavern platformer maze and writes it to XRAM.
// Sets player_start_x and player_start_y to a safe spawn position.
void maze_generate(void);

#endif // MAZE_H
